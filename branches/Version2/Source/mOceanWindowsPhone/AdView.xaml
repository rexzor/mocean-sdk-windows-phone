<UserControl x:Class="mOceanWindowsPhone.AdView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="537" d:DesignWidth="585"
	xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
	xmlns:map="clr-namespace:Microsoft.Phone.Controls.Maps;assembly=Microsoft.Phone.Controls.Maps">
    
    <Grid x:Name="LayoutRoot" >
        <map:Map HorizontalAlignment="Stretch" Name="map" VerticalAlignment="Stretch" Visibility="Collapsed"/>
        <phone:WebBrowser HorizontalAlignment="Stretch" Name="webBrowser" VerticalAlignment="Stretch" IsScriptEnabled="True" Visibility="Collapsed" />
        <MediaElement Name="mediaElement" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Collapsed"/>
		<Image Height="50" HorizontalAlignment="Right" Name="imageClose" Stretch="Fill" VerticalAlignment="Top" Width="50" Source="customclose.png" Tap="imageClose_Tap" Visibility="Collapsed" />
	</Grid>
</UserControl>
